{
  "name": "my-vuepress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "dotenv -e .env -- vuepress dev docs --temp .temp",
    "build": "vuepress build docs",
    "deploy-gh": "yarn build && bash deploy-gh.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vuepress/plugin-medium-zoom": "^1.8.2",
    "watchpack": "^2.2.0"
  },
  "dependencies": {
    "-": "^0.0.1",
    "dotenv": "^10.0.0",
    "dotenv-cli": "^4.1.0"
  }
}
